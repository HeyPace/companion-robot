name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  status-doc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify project status exists
        run: test -f PROJECT_STATUS.md
